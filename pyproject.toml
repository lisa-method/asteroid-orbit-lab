[project]
name = "asteroid-orbit-lab"
version = "0.1.0"
description = "Adaptive physical model selection for asteroid trajectory forecasting"
requires-python = "==3.14.7"
dependencies = []

[tool.uv]
package = false
